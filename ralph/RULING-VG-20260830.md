# EXTERNAL-JUDGE RULING 8 — VG certification (BINDING, prereg post-hoc Amendment 3)
Date: 2026-08-30 (KST). Trigger: self-audit HIGH flag — band/classifier inference from
cells with present_certified_cells=0, no authorizing ruling. The flag was CORRECT to raise.

## Findings of fact (judge-side full sweep: 238 receipts, 476 pad-rows, Mac-side)
1. finite: 476/476 pass. vg3 overflow substeps: 0 on 476/476.
2. vg2 (zero-record substeps) >= 601 on EVERY pad-row, including every intact trial;
   label ordering damage(med 669) < intact(717) < slip(816, max 1159) with a uniform
   ~601 floor matches physically-mandatory no-contact intervals (pre-closure approach;
   post-slip/drop separation). As implemented the bar is unsatisfiable by ANY trial:
   a spec-implementation artifact with zero discriminating information — matching your
   own diagnosis ("recording artifact, not a contact-tracking failure").
3. vg1 (>0.5 mm relative displacement): exactly 18 pad-rows = 9 receipts, ALL
   damage-labeled at F=2.0 N, a>=10 (0.51-0.61 mm) — pad-relative sliding during
   material failure is the phenomenon, not instrument failure. Max over ALL
   intact-labeled pad-rows: 0.276 mm (45% margin).

## Ruling
a. CHANNEL SCOPING: VG certifies the per-pad contact-field channel. W1 labels draw
   exclusively on the state-log channel (COM/palm kinematic residuals) and volumetric
   strain fields. Therefore, for W1 label/band/classifier inference,
   certification := finite AND vg3==0. Both hold 476/476 -> ALL 126 cells CERTIFIED.
   Classifier inference is AUTHORIZED on the confirmed labels.
b. vg2 is permanently demoted to a recorded diagnostic (bar unachievable with VBD
   soft-contact recording). No retro-computation ordered. A contact-gated dropout
   metric may be reinstated prospectively only if per-substep contact flags are stored.
c. vg1 remains binding ONLY where a pad-channel claim requires patch fidelity
   (W2-type contact-field claims). The 9 exceeding receipts are W1 damage cells whose
   pad channel supports no claim -> no censoring. W2 (F=1.2 sweep) must disclose its
   own per-cell VG summary in e2v2 artifacts; its excursion claims are descriptive of
   recorded contact geometry (judge recomputed all 36 excursions: exact match) and stand.
   HARD STOP RULE: any FUTURE vg1 violation on an intact-labeled pad-row is
   STOP + escalate — do not self-amend.

## Ordered actions
1. Record this ruling in remote ralph/DECISIONS.md and the PREREG_W1 amendment chain
   (Amendment 3, post-hoc spec correction — same class as Amendments 1-2).
2. Re-emit final band coverage under the amended rule (expect present_certified_cells
   = 42/42 per material); preserve the strict count as present_vg_strict_cells.
3. Emit the classifier verdict on confirmed labels per prereg first-match
   (expected: P-B CLOSURE — confirmed empty bands persistent at all higher tested a
   for every E). The contraction narrative remains descriptive alongside it.
4. Revert the provisional/inconclusive doc markings added during the flag.
5. W3 scene selection from final-band labels is APPROVED under (a).
6. Commit everything. No label, band value, or paper number changes under this ruling.

Judge verification basis: independent 126/126 label+seed-count match (2026-08-30),
vg full-sweep script (Mac scratchpad), e1v2_band coverage/provenance inspection.
